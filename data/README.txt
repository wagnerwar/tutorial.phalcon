O arquivo dados_iniciais.sql contém os dados necessários para rodar a aplicação.
É só rodar o seguinte comando, caso esteja logado numa sessão do MySQL pelo terminal:
mysql -uroot -p < dados_iniciais.sql
